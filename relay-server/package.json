{
  "name": "zvasp-relay-server",
  "version": "1.0.0",
  "description": "Cross-chain relay server for ZVASP verification system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test-relay.js",
    "deploy:kadena": "cd .. && npx hardhat run scripts/deploy.js --network kadenaTestnet"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ethers": "^6.8.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}